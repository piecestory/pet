# سجل تنفيذ تصميم "المعرض الكلاسيكي" — قطعة وقصة

تطبيق التصميم المُعتمد (رقم ١) على كود Next.js الفعلي. نفس الألوان والخطوط والهيكلية الأصلية — لم يتغيّر شيء في المنطق البرمجي أو الـ API أو قاعدة البيانات، فقط الطبقة البصرية والإصلاحات أدناه.

## الملفات المعدّلة (9 ملفات)

| الملف | التغيير |
|---|---|
| `app/globals.css` | إضافة نظام "اللوحة البديلة" (`.ph`, `.ph-2`, `.ph-3`) + لوحة داكنة (`.art-dark`) + توقيع زوايا الإطار الذهبية (`.frame-corners`) + زخرفة الإناء (`.vase-motif`) + شارة "نادرة" (`.rare-badge`) |
| `components/home/Hero.tsx` | إزالة الاعتماد على صورة خلفية غير موجودة (`hero-antique.jpg`)، استبدالها بلوحة داكنة + زخرفة + زوايا إطار + سطر تعريفي فوق العنوان |
| `components/home/CategoryStrip.tsx` | نفس الشيء لأيقونات التصنيفات الدائرية الست |
| `components/home/PromoBanners.tsx` | نفس الشيء لبانري المزادات والباحث الشخصي |
| `components/home/BlogPreview.tsx` | نفس الشيء لصور المدونة |
| `components/product/ProductCard.tsx` | **الأهم**: نظام `<img onError>` — يعرض الصورة الحقيقية إن وُجدت، وإلا تظهر لوحة بديلة أنيقة تلقائيًا. أضفت أيضًا خاصية `badge` اختيارية لأي منتج |
| `components/home/RareFinds.tsx` | استخدام خاصية `badge="نادرة"` الجديدة |
| `components/home/LatestProducts.tsx` / `FeaturedProducts.tsx` | تنويع درجات اللوحة البديلة بصريًا فقط |

## إصلاح أمني ⚠️

**تم حذف** `app/api/debug-env/route.ts` بالكامل. هذا المسار كان بلا أي حماية (بدون تحقق تسجيل دخول) ويكشف عند زيارته مباشرة:
- اسم مستخدم قاعدة البيانات
- أول وآخر حرفين من كلمة مرور MySQL + طولها
- عنوان السيرفر والمنفذ واسم القاعدة

كان يحمل تعليقًا في الكود نفسه يقول "يجب حذفه لأسباب أمنية" ولم يُحذف. **مهم بشكل خاص الآن** لأنك ستربط `DATABASE_URL` حقيقي قريبًا.

## فحوصات تمّت قبل التسليم

- ✅ `npm install` — نجح بدون أخطاء
- ✅ `npx tsc --noEmit` — صفر أخطاء جديدة في الملفات المعدّلة (الأخطاء الموجودة في `app/shop` و `app/admin/products` سببها فقط أن Prisma Client لم يُولَّد بعد في بيئة الفحص؛ ستختفي تلقائيًا بعد `npx prisma generate` عندك)
- ✅ `npx eslint` — صفر أخطاء في كامل المشروع (3 تنبيهات فقط بخصوص استخدام `<img>` بدل `next/image`، قرار مقصود لتفعيل اللوحة البديلة التلقائية)
- ✅ مطابقة بصرية كاملة مع معاينة "المعرض الكلاسيكي" المعتمدة، على قياسات: جوال (390px) وتابلت (768px) وديسكتوب (1440px)

## الخطوة التالية

1. فك ضغط المشروع فوق نسختك الحالية (أو استبدلها بالكامل)
2. `npm install`
3. `npx prisma generate`
4. أضف صور المنتجات الحقيقية إلى `public/images/` بنفس الأسماء المستخدمة في الكود (أو حدّث المسارات) — ستظهر تلقائيًا مكان اللوحات البديلة بدون أي تعديل إضافي
5. اربط `DATABASE_URL` الحقيقي (التفاصيل في الرسالة)

---

# مراجعة أمنية شاملة — الجزء الثاني

## 🔴 حرج — تم إصلاحه

**لوحة التحكم بالكامل كانت بدون أي حماية دخول.** أي صفحة تحت `/admin/*` (المنتجات، الطلبات، العملاء...) كانت تعرض بيانات حية من قاعدة البيانات لأي زائر يفتح الرابط مباشرة، بدون تسجيل دخول. مسارات الـ API نفسها (`/api/products` POST/PUT/DELETE إلخ) كانت محمية بشكل صحيح — المشكلة فقط في صفحات الواجهة. كما لم تكن هناك صفحة دخول للوحة التحكم أصلاً.

**الإصلاح:**
- `middleware.ts` جديد — يتحقق من صلاحية التوكن (JWT) قبل السماح بالوصول لأي صفحة تحت `/admin`، ويحوّل لصفحة الدخول تلقائيًا إن لم يكن مسجّلاً
- `app/admin/login/page.tsx` — صفحة دخول فعلية، تتحقق أيضًا أن الحساب من صلاحيات الإدارة (لا يُسمح لحساب عميل عادي بالدخول حتى لو كانت بياناته صحيحة)
- إعادة تنظيم مجلد `app/admin` باستخدام route group `(dashboard)` بحيث تبقى صفحة الدخول بدون الشريط الجانبي
- زر "تسجيل الخروج" مضاف في الشريط الجانبي (`components/admin/LogoutButton.tsx`)

## 🟠 متوسط — تم إصلاحه

| المشكلة | الإصلاح |
|---|---|
| رفع الملفات يقبل أي نوع/حجم، وتسمية الملف تعتمد على اسم الملف الأصلي (احتمال ثغرة مسار) | `app/api/upload/route.ts`: صور فقط (jpg/png/webp/gif) + حد 8MB للملف + حد 12 ملف بالطلب + اسم عشوائي آمن بالكامل |
| لا حماية من تخمين كلمة المرور بالتكرار على تسجيل الدخول | `lib/rate-limit.ts` جديد — يحظر بعد 8 محاولات فاشلة خلال 15 دقيقة لنفس البريد+IP |
| `JWT_SECRET` بدون تحقق وقت الإقلاع | فشل فوري وواضح إن كان غير معرَّف أو أقصر من 16 حرفًا، بدل خطأ غامض لاحقًا |
| Next.js 14.2.5 يحمل ثغرات معروفة (كشف كود المصدر + DoS) | تحديث إلى 14.2.35 |
| إعداد الصور يسمح بتحميل/تحسين صور من **أي** نطاق خارجي (`hostname: "**"`) — ثغرة DoS محتملة عبر واجهة `/_next/image` | تضييق `next.config.mjs` لعدم السماح بأي نطاق خارجي (لا حاجة له، كل الصور محلية) |
| مكتبة `multer` مثبّتة وغير مستخدمة إطلاقًا في الكود، وتحمل ثغرات معروفة | حذفها بالكامل من `package.json` |
| أخطاء TypeScript (implicit any) في 3 مواضع بصفحات الإدارة والمتجر | تحديد أنواع صريحة، تحقق منها فعليًا عبر `next build` كامل ناجح |

## ⚪ معلومة — بدون إصلاح فوري (قرار يستحق نقاش منفصل)

فحص `npm audit` كشف أن خط Next.js 14.x توقف عن استقبال تحديثات أمنية لعدد من الثغرات الأحدث (تحتاج ترقية لـ Next 15 أو 16 — ترقية إصدار رئيسي فيها تغييرات جذرية، مو تحديث بسيط). راجعت كل ثغرة بعنوانها مقابل الكود الفعلي عندك:

- أغلبها يخص ميزات **غير مستخدمة إطلاقًا بمشروعك**: Server Actions، WebSocket، i18n بـ Pages Router، CSP nonces — تحققت بالبحث الفعلي بالكود ولا وجود لأي منها
- المتبقي (تسمم الكاش، DoS بالـ Image Optimization) يحتاج سيناريوهات نشر أوسع (CDN، حمل عالٍ) أقل احتمالًا بمرحلتك الحالية

**توصيتي:** لا داعي للاستعجال بترقية Next 15/16 الآن — هذا مشروع منفصل بحد ذاته (فيه تغييرات جذرية في الكود). خلّه على قائمة الخطط المستقبلية بعد ما يستقر الموقع ببيانات حقيقية.

## ✅ فحوصات التحقق

- `next build` كامل (بمتغيرات بيئة تجريبية) — نجح 100% بدون أي خطأ
- `npx tsc --noEmit` — صفر أخطاء (عدا استيراد أنواع Prisma التي تحتاج `prisma generate` عندك)
- `npx eslint .` على كامل المشروع — صفر أخطاء
- `npm audit` بعد كل الإصلاحات — تم توثيق كل نتيجة أعلاه بدل تجاهلها

